services:
  - type: web
    name: xclown-music
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3001
      - key: JWT_SECRET
        generateValue: true
